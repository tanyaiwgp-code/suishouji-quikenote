# 随手记 · Windows 个人记录应用 — UI 设计规范

> 版本 v1.0 ｜ 2026-08-11 ｜ 设计：UI Designer
> 定位：快速启动、后台待机、随时记录的个人笔记应用

---

## 1. 产品定位与核心原则

| 维度 | 定义 |
|---|---|
| **目标** | 1 秒内开始输入，0 负担记录 |
| **形态** | Windows 桌面应用，常驻系统托盘 |
| **核心格式** | Markdown（.md + assets/ 图片目录）、TXT、Word 导入/导出 |
| **图文混排** | 图片拖入即存 assets/ 并插入引用，一个笔记 = 一个文件目录 |
| **数据开放** | 纯本地文件结构，不锁库，可随时迁移/备份/用其他工具打开 |

**三条设计铁律：**
1. **快**：托盘常驻 + 全局快捷键，从按键到落笔 < 1s
2. **轻**：界面只保留与"记录"相关的元素，无社交、无广告、无冗余
3. **暖**：纸感底色 + 大字号，长时间阅读不疲劳

---

## 2. 交互流程与启动链路

### 2.1 快速记录浮窗（主入口）

```
托盘图标/全局快捷键 Ctrl+Alt+N
        │
        ▼
┌─────────────────────────────────────┐
│  快速记录浮窗（置顶、无边框、居中）        │
│  ┌─────────────────────────────────┐ │
│  │ 标题输入（可选，回车即存）            │ │
│  ├─────────────────────────────────┤ │
│  │ 正文输入区（支持 MD 语法、拖入图片）    │ │
│  ├─────────────────────────────────┤ │
│  │ [图片][MD][TXT]  [保存并关闭] [打开主窗口] │ │
│  └─────────────────────────────────┘ │
└─────────────────────────────────────┘
   Esc = 丢弃关闭（有内容时先提示）
   Ctrl+Enter = 保存并关闭
   Ctrl+Shift+Enter = 保存并打开主窗口
```

**浮窗行为规范：**
- 始终置顶、焦点自动落位正文区
- 关闭即自动保存草稿到「收件箱」，未完成内容不丢失
- 浮窗尺寸约 560×420，可缩放，位置记忆
- 右下角「打开主窗口」按钮 → 收起浮窗、唤起主窗口（对应下方布局）

### 2.2 主窗口（完整工作区）

```
主窗口（默认 1280×800，记忆上次尺寸/位置）
│
├── 顶部标题栏：应用名 + 搜索框 + 主题切换 + 设置
│
└── 内容区（响应式三档，见第 3 节）
```

**启动原则**：开机自启驻托盘（可关）；首次运行开主窗口引导；之后启动默认最小化到托盘。

---

## 3. 移动优先 · 响应式布局

**策略**：以窄窗口（最小可用）为基准设计，宽度增加渐进展开信息层级。

| 断点 | 布局 | 说明 |
|---|---|---|
| **< 720px** | 单栏 | 列表 ⇄ 编辑器 切换，专注单一任务，快速记录 |
| **720–1100px** | 双栏 | 左：笔记列表；右：编辑器 |
| **> 1100px** | 三栏 | 左：导航；中：笔记列表；右：编辑器 |

### 三栏结构（宽窗）

```
┌──────┬─────────────┬──────────────────────────────┐
│ 导航  │  笔记列表     │  编辑器                        │
│ 52px │  240-300px   │  弹性 1fr                     │
│      │              │                              │
│ ≡全部 │  ⌕ 搜索…      │  工具栏：B I H 图片 代码 导出    │
│ ☆收藏 │ ┌─────────┐  │  标题输入（大字号）              │
│ #标签 │ │ 笔记卡片  │  │  ─────────────────────────   │
│      │ └─────────┘  │  正文（MD 渲染 / 源码可切换）     │
│ +新建 │  卡片列表     │  拖入图片 → 存 assets 并引用     │
│ ⚙设置 │  时间排序     │  底部状态栏：字数 · 格式 · 已保存  │
└──────┴─────────────┴──────────────────────────────┘
```

**笔记卡片规范：**
```
┌──────────────────────────┐
│ 周计划与灵感      [MD]     │ ← 标题 13-14px/500 + 格式徽章
│ 1. 能力君测试反馈整理…      │ ← 摘要 1 行省略
│ 今天 10:24 · 3 图          │ ← 元信息 11px 次要色
└──────────────────────────┘
```
- 选中态：卡片面白底 + 左侧 3px 强调色条
- 悬停态：背景轻微提亮

**导航图标**（36×36 圆角方块）：全部 ≡ ｜ 收藏 ☆ ｜ 标签 # ｜ 新建 +（置顶强调色）｜ 设置 ⚙（底部）

---

## 4. 配色系统

### 4.1 浅色 · 晴纸（默认）

| Token | 色值 | 用途 |
|---|---|---|
| `--bg` | #F5FAF8 | 页面底（清爽微绿白） |
| `--surface` | #FFFFFF | 卡片面 |
| `--ink` | #1E2A28 | 主文字（墨青黑） |
| `--ink-2` | #74817D | 次要文字 |
| `--accent` | #38A3E8 | 强调天蓝（主按钮/链接/激活） |
| `--accent-2` | #1E7BB8 | 深蓝（hover/按压） |
| `--success` | #2FBF8F | 成功薄荷（已保存/完成） |
| `--mark` | #E4F5EE | 标记底色（选中/引用块） |
| `--border` | #DCE7E3 | 边框线 |

### 4.2 深色 · 墨夜

| Token | 色值 | 用途 |
|---|---|---|
| `--bg` | #101418 | 页面底（真深墨） |
| `--surface` | #1A2026 | 卡片面 |
| `--ink` | #E8EEEC | 主文字（暖白） |
| `--ink-2` | #74807D | 次要文字 |
| `--accent` | #4FB3EA | 强调天蓝 |
| `--success` | #3ECF9C | 成功薄荷 |
| `--border` | #2A3238 | 边框线 |
| `--warn` | #F5B04A | 警示琥珀 |

### 4.3 语义色

| 语义 | 浅色 | 深色 |
|---|---|---|
| 链接 | #1E7BB8 | #6FC3EF |
| 警示 | #F0A32F | #F5B04A |
| 错误 | #E0584F | #E56B60 |
| 成功 | #2FBF8F | #3ECF9C |

### 4.4 无障碍对比度（WCAG AA）

- 正文 `#1E2A28 on #F5FAF8` ≈ 13.8:1 ✅（远超 7:1 AAA）
- 次要文字 `#74817D on #FFFFFF` ≈ 4.6:1 ✅（≥4.5:1）
- 深色主题 `#E8EEEC on #101418` ≈ 13.2:1 ✅
- 强调色仅用于大号文字/图形/非文字组件，满足 3:1 即可

---

## 5. 字体系统

### 5.1 字体栈

| 用途 | 字体栈 |
|---|---|
| 中文 | `Source Han Sans SC` → `Microsoft YaHei` 兜底 |
| 西文/数字 | `Inter` → `Segoe UI` 兜底 |
| 代码/路径 | `JetBrains Mono` → `Consolas` 兜底 |

> 注：思源黑体 SC 为开源可随包分发；Windows 10 无 Inter 时自动回退 Segoe UI，视觉一致性好。

### 5.2 字号标尺（大字号策略）

| 层级 | 字号 | 字重 | 行高 | 用途 |
|---|---|---|---|---|
| Display | 24px | 600 | 1.4 | 应用标题/欢迎页 |
| H1 标题 | 20px | 600 | 1.5 | 笔记标题 |
| 正文 | 16px | 400 | 1.8 | 编辑区正文（比默认大一号） |
| 辅助 | 13px | 400 | 1.6 | 元信息/次要文字 |
| 代码 | 14px | 400 | 1.7 | 行内代码/路径 |

**中文排版细节**：正文 `text-wrap: pretty`；标题 `text-wrap: balance`；段间距 0.5em；中文不强制两端对齐。

---

## 6. 间距 · 圆角 · 组件

### 6.1 基础 token

| Token | 值 | 说明 |
|---|---|---|
| 间距基数 | 4px | 8/12/16/24 递增 |
| 卡片圆角 | 10px | `--radius` |
| 按钮圆角 | 8px | 小控件 |
| 输入框圆角 | 9px | 搜索/标题 |
| 边框 | 0.5–1px | `--border` 色 |

### 6.2 核心组件

**按钮**
```
主要按钮：accent 底 + 白字 13px，hover 变 accent-2，按压下沉 1px
次要按钮：透明底 + 1px border + ink 字，hover 浅灰底
图标按钮：36×36 圆角方块，hover 提亮
禁用态：opacity 0.45 + cursor not-allowed
焦点态：2px accent 描边（键盘导航可见）
```

**工具栏**（编辑器顶部）
| 控件 | 行为 |
|---|---|
| B / I | 粗体/斜体 |
| H | 标题层级菜单 |
| 图片 | 插入图片（或直接拖拽） |
| 代码 | 行内/代码块 |
| ⧉ 复制 | 复制 Markdown 到剪贴板 |
| ⇪ 从 Word 导入 | 系统对话框选 .docx → 转 MD 存库（M5） |
| ⇓ 导出为 Word | 当前笔记导出 .docx（系统对话框选保存位置，M5） |

**格式徽章**：MD = 琥珀底 #F4E7D6/#B06A2A；TXT = 薄荷底 #E7EFE8/#5F7A63；DOCX = 蓝底 #E3EDF7/#3E6E9C
（M5 起库内仅 MD/TXT 笔记，DOCX 导入即转 MD；DOCX 徽章色为预留扩展，当前不出现）

**图片卡片**（编辑器内）
```
┌───────────────────────────────┐
│ ▦ 缩略图  assets/note-xxx.png   │
│          拖入的图片自动存入 assets  │
└───────────────────────────────┘
```

---

## 7. 状态与微交互

| 状态 | 规范 |
|---|---|
| 自动保存 | 输入停顿 800ms 自动保存，状态栏显示「已自动保存」 |
| 空状态 | 无笔记时显示引导插图 + 「点击 + 或按 Ctrl+Alt+N 开始记录」 |
| 加载 | 骨架屏（浅色占位块），非 spinner |
| 过渡动画 | 150ms ease；列表项进入 fade+slide 4px |
| 动效偏好 | 尊重系统 `prefers-reduced-motion`，关闭时零动画 |

---

## 8. 文件格式与存储策略

```
我的笔记/                      ← 用户可选根目录（默认 文档/随手记）
├── 收件箱/                    ← 快速记录默认落点
│   └── 2026-08-11.md
├── 项目A/
│   ├── 周计划.md
│   └── assets/                ← 该笔记的图片
│       └── note-20260811-1.png
└── 旧文档/
    └── 读书摘录.txt
```

| 格式 | 策略 |
|---|---|
| `.md` | 一等公民，全功能（图文混排、标签、MD 渲染） |
| `.txt` | 直接打开编辑，无格式 |
| `.docx` | 导入转 MD（图片入 assets，M5）+ 导出为 DOCX（M5）；不直接存库编辑（库内仅 md/txt） |

---

## 9. 设计 Token（可直接交付开发）

```css
:root {
  /* 晴纸 */
  --bg: #F5FAF8; --surface: #FFFFFF;
  --ink: #1E2A28; --ink-2: #74817D;
  --accent: #38A3E8; --accent-2: #1E7BB8;
  --success: #2FBF8F; --mark: #E4F5EE;
  --border: #DCE7E3;
  --warn: #F0A32F; --danger: #E0584F; --link: #1E7BB8;
  --radius: 10px; --radius-sm: 8px;
  --font-zh: "Source Han Sans SC","Microsoft YaHei",sans-serif;
  --font-en: Inter,"Segoe UI",sans-serif;
  --font-mono: "JetBrains Mono",Consolas,monospace;
  --fs-body: 16px; --fs-title: 20px; --fs-meta: 13px;
  --lh-body: 1.8;
}
[data-theme="dark"] {
  --bg: #101418; --surface: #1A2026;
  --ink: #E8EEEC; --ink-2: #74807D;
  --accent: #4FB3EA; --success: #3ECF9C;
  --border: #2A3238;
  --warn: #F5B04A; --danger: #E56B60; --link: #6FC3EF;
}
```

---

## 10. 无障碍与体验底线

- 全部可交互元素 ≥ 44px 点击区（含图标按钮）
- 完整键盘操作：Tab 顺序、Enter/Space 触发、Esc 关闭浮窗
- 焦点可见：2px accent 焦点环，不使用 outline:none
- 颜色不承载唯一信息（错误同时有图标/文字）
- 字号可调：支持 100%–200% 缩放不破版

---

## 附：交付检查清单

- [x] 晴纸/墨夜双主题 + 系统跟随（M0；M6 增设「跟随系统」第三态）
- [x] 托盘常驻 + Ctrl+Alt+N 全局呼出（M4）
- [x] 快速记录浮窗（置顶、自动保存、一键开主窗）（M4）
- [x] 三档响应式布局（M2）
- [x] MD/TXT/DOCX 读写链路（M5）
- [x] 图片拖入 assets 自动管理（M3）
- [x] 字号/间距/圆角全部 token 化（M0；M6 字号三档缩放）
- [x] WCAG AA 对比度全达标（M0）
- [x] 设置页（数据目录/主题/开机自启/字号，M6）
- [x] 骨架屏 + reduced-motion 降级（M6）
- [x] 键盘全流程可达 + 焦点环 + 44px 点击区（M6）
- [x] NSIS 安装器（中文简体、当前用户免管理员，M6）
- [x] 删除笔记（工具栏 / 列表右键 / 键盘 Del + 二次确认，M7）
- [x] 主窗口可编辑笔记标题（M9）
- [x] Playwright E2E 15 用例 + CI 全绿（M8）
- [x] 开源发布：MIT License + GitHub Release v0.1.0（安装器 + 绿色版，2026-08-15）
