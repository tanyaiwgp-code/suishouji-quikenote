# 发布与自动更新流程（P0-商用化）

本应用已集成 **tauri-plugin-updater** 自动更新。发新版本时需按以下流程操作，否则用户无法收到更新。

## 1. 前置：签名密钥

- 私钥：`suishouji/.tauri-keys/suishouji-updater.key`（**已 gitignore，严禁提交/泄露**）
- 公钥：`suishouji/.tauri-keys/suishouji-updater.key.pub`（已写入 `tauri.conf.json → plugins.updater.pubkey`）
- 密码：`suishouji-release-2026`（请自行妥善保管；**丢失即永远无法再发更新**）

> ⚠️ 强烈建议把私钥+密码备份到密码管理器；如泄露请立即更换密钥对并同步更新 pubkey。

## 2. 每次发版的步骤

> **推荐：全自动**（第 2.4 节）。已配好 CI workflow（`.github/workflows/release.yml`），
> 只需更新版本号并打 tag，构建/签名/latest.json/Release 全部自动完成。
> 下面的手动流程保留为备用（本地构建或 CI 故障时）。

### 2.1 更新版本号

`src-tauri/tauri.conf.json` 与 `package.json` 的 `version` 同步递增（如 `0.1.0` → `0.2.0`）。
提交并推送。

### 2.2 构建 + 签名（手动备用）

```powershell
# 在 suishouji/ 目录
$env:TAURI_SIGNING_PRIVATE_KEY = (Get-Content .tauri-keys/suishouji-updater.key -Raw)
$env:TAURI_SIGNING_PRIVATE_KEY_PASSWORD = "suishouji-release-2026"
npm run tauri build   # 产物：src-tauri/target/release/bundle/nsis/*-setup.exe
```

- `tauri.conf.json` 已设 `bundle.createUpdaterArtifacts: true`,构建时**自动生成** `latest.json` 与 `*-setup.exe.sig` 签名文件(在 `bundle/nsis/` 下),无需手写。
- 若未设置签名环境变量,构建会跳过签名(updater 将不可用),务必按上面设置。

### 2.3 生成 latest.json（手动备用）

```powershell
# 用 tauri-cli 生成更新清单（需先构建出 .sig）
node node_modules/@tauri-apps/cli/tauri.js signer sign -f `
  -p $env:TAURI_SIGNING_PRIVATE_KEY_PASSWORD `
  src-tauri/target/release/bundle/nsis/随手记_0.2.0_x64-setup.exe.sig

# 或手写 latest.json（版本、URL、签名三项是核心）：
# {
#   "version": "0.2.0",
#   "notes": "更新内容简述（会显示在更新确认框）",
#   "pub_date": "2026-08-16T00:00:00Z",
#   "platforms": {
#     "windows-x86_64": {
#       "signature": "<上一步生成的签名>",
#       "url": "https://github.com/tanyaiwgp-code/suishouji-quikenote/releases/download/v0.2.0/随手记_0.2.0_x64-setup.exe"
#     }
#   }
# }
```

### 2.4 自动发布（推荐，只需两步）

```bash
# 1. 更新版本号（见 2.1）并提交推送
# 2. 打 tag 推送 —— CI 自动完成构建 + 签名 + 上传 Release
git tag v0.2.0
git push origin v0.2.0
```

CI 会：`npm ci → check → tauri build（含签名 + latest.json）→ 创建 GitHub Release 并上传安装器 + .sig + latest.json`。
之后到 Actions 页确认 job 变绿即可，无需手动上传任何文件。

> ⚠️ Release 采用**直接发布**（非草稿），因为自动更新的 endpoint 指向 `releases/latest/download/latest.json`，
> 草稿资产的下载路径不可用。若想人工把关，可临时把 `release.yml` 的 `releaseDraft` 改为 `true`，但务必记得手动 Publish。

## 3. CI 所需的 GitHub Secrets（一次性配置）

在仓库 **Settings → Secrets and variables → Actions → New repository secret** 添加：

| Secret 名 | 值 | 说明 |
|---|---|---|
| `TAURI_SIGNING_PRIVATE_KEY` | `suishouji/.tauri-keys/suishouji-updater.key` **文件全文**（含 `-----BEGIN PRIVATE KEY-----` 到 END 的多行内容，粘贴时保留换行） | 更新签名私钥；泄露=别人可伪造你的更新包 |
| `TAURI_SIGNING_PRIVATE_KEY_PASSWORD` | `suishouji-release-2026` | 私钥密码 |

> `GITHUB_TOKEN` 由 GitHub 自动提供（workflow 里已声明 `permissions: contents: write`），无需配置。
>
> ⚠️ Secret 值在 GitHub 后台加密存储，只有 CI 运行时可见；但请勿在 Issue/PR/日志中泄露私钥内容。

## 4. 静默失败策略

- 网络不可达 / GitHub 被墙：`checkForUpdates` 捕获异常并静默跳过（`log::error` 记录），不打扰用户。
- 下载失败：提示用户稍后重试或到官网手动下载。
- 安装采用 `installMode: passive`（静默进度条，不弹 UAC）。

## 5. 崩溃日志（tauri-plugin-log + panic hook）

- 运行日志：`%APPDATA%\com.suishouji.app\logs\suishouji.log`
- 崩溃日志：`%APPDATA%\com.suishouji.app\logs\crash.log`（panic 时写入）
- 前端启动后若检测到 crash.log 存在，会提示用户日志位置（仅提示一次）。
- 后续接入遥测时，可在 `get_crash_report` 基础上增加上传逻辑。
