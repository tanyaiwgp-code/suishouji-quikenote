# 随手记 · 技术选型对比与分步实施方案

> 版本 v1.0 ｜ 2026-08-11 ｜ 工程：工序达（Engineering Workflow Coach）
> 上游依据：`设计规范文档.md` v1.0（UI Designer）
> 状态：**已确认**（技术栈 Tauri 2，2026-08-11 用户拍板冻结）→ 进入 M0

---

## 开发进度（截至 2026-08-14）

| 里程碑 | 状态 | 完成日期 | 备注 |
|---|---|---|---|
| **M0** 环境搭建与骨架 | ✅ 完成 | 2026-08-12 | Rust 1.97/Node 24/MSVC/WebView2 就绪；Token 双主题；CSP 安全配置 |
| **M1** 文件系统核心 | ✅ 完成 | 2026-08-13 | 33 单测全过；扫描/原子写/frontmatter/编码/路径防护/文件锁/IPC 白名单 |
| **M2** 主窗口三栏 UI | ✅ 完成 | 2026-08-13 | 三档响应式、列表卡片、搜索倒排索引、notify 文件监听、空状态 |
| **M3** 编辑器与图文混排 | ✅ 完成 | 2026-08-13 | CodeMirror 6 + markdown-it 预览(html:false) + 图片拖入 assets + 800ms 自动保存 |
| **M3.5** 代码审视与加固 | ✅ 完成 | 2026-08-14 | 全量审视 M0-M3（安全/功能/性能）；修复 S1/B1/B2/B4/B5/S2/B7/B8 共 7 项并验证；Rust 单测 40→43 |
| **M4 前置** editor.ts 工厂化 | ✅ 完成 | 2026-08-14 | 单例→`createEditor(target)` 多实例；窗口关窗接线移至入口；无行为变化（43 单测 + 真实 DOM 冒烟零错误） |
| **M4** 浮窗托盘快捷键 | ✅ 完成 | 2026-08-14 | 托盘+菜单（含开机自启勾选）、Ctrl+Alt+N 全局快捷键、快速记录浮窗（精简壳草稿自动落盘/标题入 frontmatter）、单实例、窗口状态记忆、主窗口 X→托盘 |
| **M3.5 维护性基建** | ✅ 完成 | 2026-08-14 | 解决审视遗留 4 项风险：Vitest 前端单测（25 项）、ts-rs 生成 types.gen.ts + 命令名契约测试、错误结构化（CommandError/ApiError 8 码）、门禁（clippy/eslint/hooks/CI）；Rust 单测 43→46 |
| **M5** 格式链路 | ⬜ 待办 | — | |
| **M6** 打磨发布 | ⬜ 待办 | — | |

> 当前目标：**M5**（格式链路：TXT 直编 + DOCX 导入导出）。备份基线：git 提交 `M3.5 维护性基建`（2026-08-14）。
> **M3.5 维护性基建（2026-08-14，M5 前置）**：① **前端测试**——Vitest 纯逻辑单测（search/store/api/contract 共 25 项，mock @tauri-apps/api/core，node 环境）；② **类型防漂移**——ts-rs 从 `core/model.rs` 生成 `src/lib/types.gen.ts`（`cargo test` 重写，改模型即同步），命令名 4 清单（api.ts/commands.rs/build.rs/capabilities）由 `contract.test.ts` 契约测试锁定；③ **错误结构化**——后端 `Result<T, CommandError>`（`{code,message}`，Error 枚举 8 变体映射 snake_case code），前端 `ApiError.from` 三形态解析；④ **门禁**——`npm run check`（tsc+eslint+vitest）、`cargo clippy -- -D warnings`（[lints] 段）、git hooks（`.githooks/pre-commit` 全量）、预留 `.github/workflows/ci.yml`。Rust 单测 43→46，clippy/eslint 零警告。
> **M4 完成（2026-08-14）**：托盘常驻（左键=快速记录/右键菜单）、全局快捷键 Ctrl+Alt+N → 浮窗、快速记录浮窗（无边框置顶 560×420，时间戳文件名 + 标题入 frontmatter，Ctrl+Enter/Ctrl+Shift+Enter/Esc，关闭自动保存草稿）、单实例、开机自启开关、窗口状态记忆；主窗口 X 改为隐藏到托盘。4 个官方插件（global-shortcut/single-instance/autostart/window-state，全 Rust-only）。**顺带修复 M4 前置遗留 bug**：主窗口关窗的 `destroy()` 因缺 `core:window:allow-destroy` 被 ACL 静默拒绝（实际关不掉），改为 `hide()` 并补 `allow-hide`。43 单测全绿 + 双入口 build + 真实应用 CDP 验证（两窗口、命令、hide 均通）。
> M4 前置（2026-08-14）：`editor.ts` 工厂化完成——`createEditor(target)` 返回独立编辑器实例（快速记录浮窗复用第二实例），窗口关窗接线（onCloseRequested/beforeunload → flushSave）移至 main.ts 入口，模块导入零副作用。
> M3.5 审视加固（2026-08-14）：S1 预览链接白名单 / B1 锁泄漏 / B2 关窗落盘 / B4 孤儿 assets / B5 文件名碰撞 / S2 主题防闪 / B7 自写事件抑制 / B8 前缀读取，已在真实 Tauri 应用验证。

---

## 第一部分：技术选型对比

### 1.1 候选方案总览

| 维度 | **Tauri 2**（Rust+WebView2） | Electron（Node+Chromium） | WPF / WinUI3（.NET） | PySide6（Python） |
|---|---|---|---|---|
| 冷启动到可输入 | **~0.3–0.6s** ✅ | 1.5–2.5s ❌ | 0.4–0.8s ✅ | 1–2s ⚠️ |
| 常驻内存（托盘挂起） | **~40–80MB** ✅ | 200–350MB ❌ | 60–120MB ✅ | 120–200MB ⚠️ |
| 安装包体积 | **~8–15MB** ✅ | 80–150MB ❌ | 30–80MB ⚠️ | 60–150MB ⚠️ |
| 托盘/全局快捷键/开机自启 | 官方插件，成熟 ✅ | 生态成熟 ✅ | 原生支持 ✅ | 第三方库，较弱 ⚠️ |
| 前端技术（HTML/CSS/JS） | ✅ 直接使用设计 Token | ✅ | ❌ 需重写 XAML | ⚠️ Qt 样式，难 1:1 |
| 设计规范还原度（Token/字体/响应式） | **最高**（CSS 即规范） | 最高 | 低（XAML 转换损耗） | 低 |
| 编辑器生态（MD/CodeMirror） | ✅ Web 生态全可用 | ✅ | ⚠️ 需自研/重封装 | ⚠️ |
| 你的技能匹配度 | 前端已熟悉；需学 Rust 基础 | 前端直通 | 需学 C#/XAML | Python 直通 |
| 数据开放（文件即库） | ✅ Rust std::fs 直接 | ✅ | ✅ | ✅ |

### 1.2 关键结论

- **唯一同时满足设计铁律「快 + 轻」的现代方案是 Tauri 2**。Electron 常驻内存是 Tauri 的 3–5 倍，直接违背「后台待机」的定位；"1 秒内开始输入"在 Electron 上不成立。
- 设计规范是 CSS Token 形态（`--bg`、`--accent`…），**Tauri 前端可直接 1:1 消费，零转换损耗**；WPF 需要翻译成 XAML Resource，响应式三档布局也要重写。
- 你已有 Chrome 插件（JS/TS）开发经验，Tauri 前端的 Vite + TypeScript 是同一套心智；Rust 只负责薄薄一层文件系统与窗口管理，不写复杂业务逻辑。
- **PySide6 的真实成本**：打包体积大、WebView 渲染弱、全局快捷键/托盘需第三方库拼装，维护成本最高。你的 Python 背景在这里收益有限。
- **Tauri 2 的代价**：需要安装 Rust 工具链（约 1.5GB，一次性）；Windows 10 需确认已安装 WebView2 Runtime（Win11 自带，Win10 大概率已随 Edge 安装）。

### 1.3 推荐

```
技术栈：Tauri 2（Rust 后端 + Vite + TypeScript + vanilla/轻量框架 前端）
渲染：  系统 WebView2（不打包 Chromium）
编辑器：CodeMirror 6（源码编辑） + markdown-it（预览渲染）
DOCX：  Rust 端 pandoc 或 docx-rs（导出）/ 解析导入
存储：  纯本地文件系统（.md + assets/），零数据库
```

> ⚠️ **一个诚实的提醒**：如果你完全不想碰 Rust，退而求其次的务实选择是 **WPF + Markdig**（纯 C#，无需 WebView 依赖），代价是设计还原度下降、UI 迭代慢一些。若你接受「装 Rust 换 3–5 倍性能与体积优势」，Tauri 是正确选择。

---

## 第二部分：架构设计（以 Tauri 2 为准）

### 2.1 分层结构

```
┌─────────────────────────────────────────────┐
│ 前端层（Vite + TS）                           │
│  ├─ UI：三栏布局 / 快速记录浮窗 / 托盘菜单       │
│  ├─ 状态：轻量 store（nanostores，无重型框架）   │
│  ├─ 编辑：CodeMirror 6（源码）+ 预览（markdown-it）│
│  └─ 样式：设计 Token → CSS 变量（晴纸/墨夜）     │
├─────────────────────────────────────────────┤
│ IPC 契约层（tauri::command，Rust）            │
│  ├─ notes:list / read / write / rename / delete
│  ├─ assets:import（图片拖入 → assets/）
│  ├─ docx:import / export
│  └─ window:quicknote / main / tray
├─────────────────────────────────────────────┤
│ 核心层（Rust）                                │
│  ├─ fs_store：目录扫描、笔记模型、原子写       │
│  ├─ markdown：frontmatter 解析（标题/标签/时间）│
│  ├─ docx_bridge：docx ⇄ md 转换              │
│  └─ sys：托盘、全局快捷键、开机自启、单实例锁    │
└─────────────────────────────────────────────┘
```

### 2.2 数据模型（笔记条目）

```ts
interface NoteMeta {
  id: string          // 相对路径哈希
  title: string       // frontmatter title，无则取文件名
  path: string        // 相对根目录路径
  format: "md" | "txt"
  mtime: number
  imageCount: number
  tags: string[]
  pinned: boolean
  preview: string     // 首行截断 80 字
}
```

### 2.3 关键设计决策（ADR 摘要）

| # | 决策 | 理由 |
|---|---|---|
| 1 | 文件即库，无 SQLite | 数据开放、可迁移、可用任意工具打开 |
| 2 | 笔记元数据存 frontmatter | 标题/标签/置顶不污染正文，且 MD 规范 |
| 3 | 图片存 `笔记名/assets/` 而非全局 assets | 一个笔记一个目录，移动/备份不散落 |
| 4 | 自动保存 = 800ms 防抖写盘 + 原子写（tmp+rename） | 防崩溃丢数据 |
| 5 | 单实例锁（第二实例激活首个窗口） | 全局快捷键只注册一次 |
| 6 | 快速记录浮窗 = 独立无边框窗口 | 置顶/缩放/位置记忆互不干扰 |

---

## 第三部分：分步实施计划

> 原则：**小步、原子、每步可验证**。每个里程碑都有明确验收标准，验收不过不进入下一步。

### M0 · 环境搭建与项目骨架（半天）

| 任务 | 验收标准 |
|---|---|
| 安装 Rust 工具链 + WebView2 Runtime 检查 | `rustc --version` 通过；`WebView2Loader` 运行无报错 |
| `create-tauri-app` 脚手架（Vite+TS） | `npm run tauri dev` 弹出空白窗口 |
| 落地设计 Token（晴纸/墨夜 CSS 变量 + data-theme 切换） | 切换按钮可实时换肤，对比度按规范 |
| WebView2 安全配置：CSP 头、禁用 eval/Node 集成 | Tauri 配置中设置严格 CSP；`dangerousRemotePageIpc` 等危险特性确认关闭 |

### M1 · 文件系统核心（Rust 纯逻辑，先测后写）（1 天）

| 任务 | 验收标准 |
|---|---|
| `fs_store`：扫描根目录 → NoteMeta 列表 | **单测**：建 20 个 .md/.txt 随机目录，返回排序正确 |
| 读写：read / write / 原子写 | 单测：写后读一致；模拟崩溃后 tmp 不残留 |
| frontmatter 解析（title/tags/pinned/mtime） | 单测：含/不含 frontmatter 均正确降级 |
| 图片计数：扫描 `assets/` | 单测：0/1/多图正确 |
| 路径遍历防护：所有文件操作前 canonicalize + 前缀校验 | 单测：构造 `../` 恶意路径被拒绝；越界读写返回错误 |
| IPC 权限模型：按最小权限配置 Tauri capability | 前端仅可调用白名单内的 command；非白名单 invoke 被拒绝 |
| 文件锁：防止浮窗与主窗口同时编辑同一文件 | 单测：同文件二次打开返回锁冲突；关闭后锁释放 |
| 编码兼容：自动检测 UTF-8 / GBK / GB2312 | 单测：GBK 编码 .txt 读取不乱码；无 BOM UTF-8 正常 |

### M2 · 主窗口三栏 UI（1–1.5 天）

| 任务 | 验收标准 |
|---|---|
| 三档响应式布局（<720 单栏 / 720-1100 双栏 / >1100 三栏） | 拖拽窗口宽度，三档正确切换无破版 |
| 导航栏 + 笔记列表 + 卡片组件 | 列表渲染真实文件；选中态/悬停态符合规范 |
| 搜索框（标题+全文过滤） | 输入即过滤，中文无乱码 |
| 文件系统监听（`notify` crate）：外部修改自动刷新 | 用其他编辑器改文件后，列表和编辑器自动更新 |
| 搜索倒排索引：标题+正文前 500 字内存缓存 | 200 条笔记内搜索延迟 <50ms |
| 空状态引导页 | 空目录显示引导插图与快捷键提示 |

### M3 · 编辑器与图文混排（1.5–2 天）

| 任务 | 验收标准 |
|---|---|
| CodeMirror 6 源码编辑 + MD 高亮 | 打开 .md 可编辑，语法高亮正确 |
| 预览模式（markdown-it）+ 双栏对照 | 编辑 ↔ 预览实时同步；**安全验收：markdown-it 禁用 HTML 标签（`html: false`），`<script>` 等不执行** |
| 工具栏：B/I/H/代码/导出 | 插入语法到光标处 |
| 图片拖入 → Rust `assets:import` → 插入引用 | 拖入后自动生成 `assets/note-xxx.png` 并插入 `![](...)`，重载仍显示；**单张 ≤20MB、单笔记 ≤50 张，超限友好提示** |
| 自动保存（800ms 防抖 + 原子写） | 停顿 0.8s 后状态栏出现「已自动保存」 |

### M4 · 快速记录浮窗 + 托盘 + 全局快捷键（1 天）

| 任务 | 验收标准 |
|---|---|
| 托盘常驻 + 菜单（新建/打开/退出） | 关闭窗口后进程存活，托盘可唤出 |
| 全局快捷键 Ctrl+Alt+N → 浮窗 | 任意应用内按下即唤出置顶浮窗 |
| 浮窗：标题+正文+图片+保存逻辑 | Esc 有内容时提示；Ctrl+Enter 保存到收件箱 |
| 「打开主窗口」按钮 | 收起浮窗并唤起主窗口 |
| 开机自启开关（可关闭） | 设置里可开关，注册表写入/删除正确 |
| 单实例锁 | 二次启动不重复注册快捷键，激活现有窗口 |

### M5 · 格式链路：TXT 直编 + DOCX 导入导出（1–1.5 天）

| 任务 | 验收标准 |
|---|---|
| .txt 直接打开编辑（纯文本，无 MD 高亮） | 读写往返内容一致（含 UTF-8 BOM 兼容） |
| DOCX 导入 → 转 MD（标题/段落/图片/表格） | 样例 docx 转换后结构正确，图片入 assets；**安全验收：XML 解析禁 DTD/ENTITY（防 XXE）；解压前限制总大小 ≤50MB（防 ZIP 炸弹）；导入图片不发起网络请求** |
| MD 导出 → DOCX | 生成 docx 可被 Word/WPS 打开，中文不乱码 |
| 格式徽章（MD/TXT/DOCX） | 列表按扩展名显示对应徽章色 |

### M6 · 打磨与发布（1–2 天）

| 任务 | 验收标准 |
|---|---|
| 设置页（根目录选择/主题/开机自启/字号） | 持久化到 config，重启生效 |
| 骨架屏 + 过渡动画 + reduced-motion 降级 | 加载无白屏；系统减弱动效时零动画 |
| 键盘可达性 + 焦点环 + 44px 点击区 | Tab 全流程可达，焦点环可见 |
| 打包（NSIS 安装器） | 安装后托盘常驻、快捷键可用、版本号正确 |

---

## 第四部分：验证与风险

### 4.1 验证策略

| 层 | 手段 |
|---|---|
| Rust 核心 | `cargo test` 单元测试（M1 起每步必跑）+ `cargo clippy --all-targets -- -D warnings` 零警告 |
| 前端逻辑 | `npm run check` = `tsc --noEmit` + `eslint src` + `vitest run`（纯逻辑单测，M3.5 起） |
| IPC 契约 | `contract.test.ts` 锁定命令名 4 清单一致；`api.test.ts` 验证参数形状 |
| 类型契约 | ts-rs 生成 `types.gen.ts`（`cargo test` 重写，CI 校验与入库一致） |
| UI | 手工验收清单（每里程碑附于 PR）+ headless 真实 DOM 冒烟（临时） |
| 端到端 | 真实应用 CDP 冒烟（临时）；最后跑设计规范附录「交付检查清单」8 项全勾 |

### 4.2 主要风险与回退

| 风险 | 概率 | 缓解 / 回退 |
|---|---|---|
| Rust 工具链安装失败（网络/磁盘） | 低 | 国内镜像 `rsproxy.cn`；失败则回退 WPF 方案 |
| **MSVC C++ Build Tools 缺失（环境实测发现）** | 中 | Tauri 官方仅支持 MSVC；需装 VS Build Tools VCTools 工作负载（2-4GB，UAC 确认）。**本机无任何包管理器，只能手动安装** |
| Win10 WebView2 缺失 | 低 | 已实测本机安装 151.x ✅（无需处理） |
| DOCX 转换边界 case 多 | 中 | 先支持"标题/段落/图片/表格"四类，其余降级为纯文本块，文档注明限制 |
| 全局快捷键冲突 | 中 | 设置页提供改键；冲突时静默失败并在托盘提示 |

---

## 附录：估算汇总

| 里程碑 | 工期 | 前置依赖 |
|---|---|---|
| M0 骨架 | 0.5 天 | 技术栈确认 |
| M1 文件核心 | 1 天 | M0 |
| M2 主窗口 UI | 1–1.5 天 | M1 |
| M3 编辑器图文 | 1.5–2 天 | M2 |
| M4 浮窗托盘 | 1 天 | M2 |
| M5 格式链路 | 1–1.5 天 | M1 |
| M6 打磨发布 | 1–2 天 | M3/M4/M5 |
| **合计** | **7–9.5 天** | |

> 注：按单人全栈推进估算；先做 M0–M4（核心体验闭环），M5/M6 可后置。
